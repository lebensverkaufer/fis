import tkinter as tk
from tkinter import *

import new_variable as varFrame
import new_lTerm as ltFrame

def mainFrame(appFrame):
    mainWizard = tk.Frame(appFrame)
    mainWizasrd.pack()
    Label(mainWizard, text="here be main Wizard").pack(side=LEFT)
    return mainWizard
